export const DesignPage = () => {
  return (
    <div>
      create the preview for first page as seperate component and call that here, and only update the slides,
      the next button should be disabled, or maybe it should open the same modal as save
    </div>
  )
}
